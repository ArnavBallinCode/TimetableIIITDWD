[pytest]
markers =
    blackbox: boundary and equivalence partition tests for scheduler behavior
    whitebox: branch and decision-path tests for conflict-resolution logic
    integration: multi-module or script-level integration tests
    regression: tests that protect previously working end-to-end behavior
